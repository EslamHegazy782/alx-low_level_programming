#include "main.h"
/**
 * _abs - printing the absolute value
 *
 * @i: input as integar
 *
 * Return: absolute value
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
